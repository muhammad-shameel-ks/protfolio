# AGENTS.md

## Agent skills

### Issue tracker

Issues and specs live as GitHub issues, tracked with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context — `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
