---
import Layout from '../layouts/Layout.astro';
import Shell from '../components/Shell';
import Hero from '../components/react/Hero';
import FloatingShapes from '../components/react/FloatingShapes';
import StoryBlock from '../components/react/StoryBlock';
import TextReveal from '../components/react/TextReveal';
import ProjectList from '../components/react/ProjectList';
import ProxyProofStrip from '../components/react/ProxyProofStrip';
import FAQ from '../components/react/FAQ';
import PortraitPlaceholder from '../components/react/PortraitPlaceholder';
import ContactForm from '../components/react/ContactForm';
import InfraFlow from '../components/react/InfraFlow';
import TechStack from '../components/react/TechStack';
import Footer from '../components/react/Footer';

export const prerender = true;
---

<Layout title="Shameel — DevOps in Progress">
	<!-- Decorative floating background shapes -->
	<FloatingShapes client:load />

	<Shell client:load>
	<div class="relative z-10">
		<!-- Hero section -->
		<Hero client:load />

		<!-- Chapter 01 — Origin story: builds trust, shows self-taught grit -->
		<section id="chapter-01">
			<StoryBlock
				client:visible
				icon="rocket"
				eyebrow="Chapter 01 — The Origin"
				heading="I didn't start with a CS degree. I started with curiosity and a broken laptop."
				body="Most people discover programming through a structured curriculum. I discovered it by trying to make my computer do things it wasn't supposed to. That broken laptop eventually ran Arch Linux, then a tiling window manager, then a custom framework I built called Barchy. Somewhere along the way, I realized — if I can bend an OS to my will, I can probably build software too."
				aside="Fun fact: I daily drive Arch Linux with a custom Hyprland setup. I built <span class='text-accent font-semibold'>Barchy Reborn</span> — a leaner adaptation of <span class='text-accent font-semibold'>Omarchy</span> (the famous <span class='text-accent font-semibold'>DHH</span> setup). Yes, I use Arch. No, I won't stop mentioning it."
			/>
		</section>

		<TextReveal
			client:visible
			text="I believe the best software is built by people who are annoyed enough by a problem to solve it themselves — and stubborn enough to ship it."
			highlightWords={["annoyed", "stubborn", "ship"]}
		/>

		<!-- Chapter 02 — Philosophy: shows product thinking, user empathy -->
		<section id="chapter-02">
			<StoryBlock
				client:visible
				warm={true}
				icon="compass"
				eyebrow="Chapter 02 — The Philosophy"
				heading="No bloat. No fluff. Just things that work."
				body="I don't build software to impress other engineers. I build it to make someone's Tuesday afternoon slightly less painful. Whether that's a retail manager tracking inventory across 5 branches, or a college superintendent generating exam seating for 2000 students — if the tool saves them time and headache, I've done my job."
				aside="My operating philosophy is borrowed from my Linux setup: if it doesn't serve a purpose, it doesn't belong. Every <span class='text-accent font-semibold'>function</span>, every <span class='text-accent font-semibold'>component</span>, every line of YAML."
			/>
		</section>

		<!-- Chapter 03 — SysAdmin: differentiator, shows depth beyond frontend -->
		<section id="chapter-03">
			<StoryBlock
				client:visible
				icon="cpu"
				eyebrow="Chapter 03 — DevOps in Progress"
				heading="I'm not where I want to be. But I'm building toward it."
				body="Most devs deploy to Vercel and forget about it. I chose the harder path — a self-hosted Kubernetes cluster on a Sony VAIO, sitting on my desk. Pi-hole for DNS. PocketBase for backends. Tailscale for zero-trust access. GitHub Actions that build containers and deploy through encrypted tunnels. No cloud dashboard. No vendor lock-in. Just me, Arch Linux, and a lot of YAML."
				aside="The cluster crashed at 2 AM last week. I learned more from that incident than any tutorial could teach. That's the point — <span class='text-accent font-semibold'>I'm building in public</span>, failing forward, and documenting the journey."
			/>
		</section>

		<!-- Projects — the proof -->
		<section id="projects">
			<ProjectList client:visible />
		</section>

		<!-- Proxy proof strip — verifiable social proof (checklist #9) -->
		<ProxyProofStrip client:visible />

		<!-- Infra pipeline - shows DevOps chops -->
		<InfraFlow client:load />

		<!-- Chapter 04 — AI workflow + toolkit: multiplier mindset, pragmatic tool choice -->
		<section id="chapter-04">
			<StoryBlock
				client:visible
				warm={true}
				icon="brain"
				eyebrow="Chapter 04 — The Multiplier"
				heading="AI doesn't replace me. It makes me dangerous."
				body="I use AI as a force multiplier, not a crutch. Claude for architectural decisions and code review. Copilot for boilerplate I'd rather not type. n8n workflows that automate the boring parts of development and operations. The result: I ship at the speed of a small team while maintaining the quality standards of someone who actually reads their own diffs — and stays ruthless about tool choice: Next.js when I need SSR, Flutter for one mobile codebase, Supabase for realtime without lock-in, Python for quick automation, Kubernetes when I need to feel something."
				aside="This entire portfolio was built in a single <span class='text-accent font-semibold'>AI-augmented session</span>. The design system, the animations, the storytelling — all coordinated between human taste and machine throughput."
			/>
		</section>

		<!-- Tech stack grid -->
		<TechStack client:visible />

		<!-- Chapter 05 — The Now: current experience, verifiable -->
		<section id="chapter-05" class="px-6 md:px-12 lg:px-20 py-16 md:py-20">
			<div class="max-w-5xl mx-auto">
				<span class="font-[Silkscreen] text-[12px] tracking-widest uppercase text-accent">Chapter 05 — The Now</span>
				<h2 class="text-2xl md:text-3xl font-extrabold tracking-tight text-fg mt-2 mb-8">Where I am right now.</h2>
				<div class="space-y-7 border-l border-border pl-6 relative">
					<div class="absolute left-0 top-1 bottom-1 w-px bg-gradient-to-b from-accent/40 via-border to-transparent pointer-events-none" aria-hidden="true"></div>

					<div class="relative">
						<span class="absolute -left-[29px] top-1 w-2.5 h-2.5 rounded-full bg-accent border-2 border-bg" aria-hidden="true"></span>
						<div class="flex flex-wrap items-baseline gap-2">
							<span class="font-mono text-xs font-bold text-accent">Aug 2026 — Present</span>
							<span class="text-sm font-bold text-fg">SeeHive Hive — AI Agentic Developer</span>
							<span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full bg-pastel-green/40 border border-green-200/40 text-[10px] font-bold tracking-wider uppercase text-green-700">Now</span>
						</div>
						<p class="text-sm leading-relaxed text-fg-muted mt-2">Building autonomous workflows and tool-use agents. Shipping with AI as a force multiplier — focus on reliability and fast iteration.</p>
					</div>

					<div class="relative">
						<span class="absolute -left-[29px] top-1 w-2.5 h-2.5 rounded-full bg-white border-2 border-border" aria-hidden="true"></span>
						<div class="flex flex-wrap items-baseline gap-2">
							<span class="font-mono text-xs font-semibold text-fg-faint">Nov 2025 — Jul 2026</span>
							<span class="text-sm font-bold text-fg">Kerala State Development Corporation for SC/ST — Software Developer</span>
						</div>
						<p class="text-sm leading-relaxed text-fg-muted mt-2">Built <a href="https://github.com/muhammad-shameel-ks/ksdc-smart-helper" class="underline underline-offset-4 hover:text-accent">KSDC Smart Helper</a> — SQL tooling used daily by non-technical staff. React, TypeScript, Node, MSSQL.</p>
					</div>

					<div class="relative">
						<span class="absolute -left-[29px] top-1 w-2.5 h-2.5 rounded-full bg-white border-2 border-border" aria-hidden="true"></span>
						<div class="flex flex-wrap items-baseline gap-2">
							<span class="font-mono text-xs font-semibold text-fg-faint">2022 — 2027 (expected)</span>
							<span class="text-sm font-bold text-fg">BSc Computer Science</span>
							<span class="text-xs text-fg-faint">— non-linear path, completing alongside production work</span>
						</div>
						<p class="text-sm leading-relaxed text-fg-muted mt-2">Shipped <a href="/projects/scentance" class="underline underline-offset-4 hover:text-accent">Scentance</a> (live e-commerce, real revenue) and a homelab K3s cluster on a VAIO while finishing the degree.</p>
					</div>
				</div>
			</div>
		</section>

		<!-- FAQ — 5 hire questions + internal links -->
		<FAQ client:visible />

		<!-- Portrait placeholder + location — team photo adapt (#10) + maps adapt (#20) -->
		<section class="px-6 md:px-12 lg:px-20 py-16 md:py-20 bg-surface/30 border-y border-border/60">
			<div class="max-w-5xl mx-auto grid md:grid-cols-2 gap-10 items-center">
				<PortraitPlaceholder client:visible />
				<div>
					<span class="font-[Silkscreen] text-[12px] tracking-widest uppercase text-accent">Palakkad, Kerala — remote worldwide</span>
					<h2 class="text-2xl md:text-3xl font-extrabold tracking-tight text-fg mt-2">The human behind the code.</h2>
					<p class="text-sm leading-relaxed text-fg-muted mt-3">Solo builder, Linux tinker, DevOps-in-progress. I’m based in Palakkad, Kerala and work async with teams across the world — IST overlap with EU/MENA, morning overlap with US East. No office — just a VAIO, Arch, and a lot of shipping.</p>
					<div class="mt-6 flex flex-wrap gap-2 text-xs">
						<a href="/projects/scentance" class="px-3 py-2 rounded-full bg-white border border-border hover:border-accent/40 transition-colors">Read Scentance case study</a>
						<a href="/projects/stock-salt" class="px-3 py-2 rounded-full bg-white border border-border hover:border-accent/40 transition-colors">Stock Salt case study</a>
						<a href="/projects/office-pal" class="px-3 py-2 rounded-full bg-white border border-border hover:border-accent/40 transition-colors">Office Pal case study</a>
					</div>
					<div class="mt-6 flex flex-wrap gap-3 text-xs text-fg-faint">
						<a href="/privacy" class="underline underline-offset-4 hover:text-accent">Privacy</a>
						<span>·</span>
						<a href="/minimal" class="underline underline-offset-4 hover:text-accent">Minimal version</a>
						<span>·</span>
						<a href="/sitemap.xml" class="underline underline-offset-4 hover:text-accent">Sitemap</a>
					</div>
				</div>
			</div>
		</section>

		<!-- Chapter 06 — The Connection -->
		<section id="contact">
			<ContactForm client:visible />
		</section>

		<!-- Footer -->
		<Footer client:visible />
	</div>
	</Shell>
</Layout>
